.playwright-mcp/
.codex-*

# Docs / working notes — keep untracked. README.md, the Sponza scene README,
# and docs/CHANGELOG.md stay tracked; everything else *.md is ignored. The
# 4-line /docs/ dance is required: un-ignore the dir, re-ignore its contents,
# then whitelist the file — a bare negation can't reach inside an ignored
# directory.
*.md
!README.md
!Sponza/README.md
/docs/
!/docs/
/docs/*
!/docs/CHANGELOG.md

# Local debug/WIP pages — the public repo and GitHub Pages ship only the
# Sponza demo (index.html).
/debug/

# Bistro (Amazon Lumberyard Bistro via NVIDIA ORCA, CC BY 4.0) is ~191MB — far
# too large for a public repo that people clone for a 100KB library. Sponza is
# committed because it is 52MB and is THE reference scene; Bistro is opt-in and
# loaded from disk. index.html reports a clear status if it is absent.
/Bistro/

node_modules/

*.log
*.tmp
*.bak

.env
.env.*
!.env.example

.DS_Store
Thumbs.db
desktop.ini

/dist/
/build/
/.cache/

# Smoke tests stay local-only (npm test runs them from the working copy).
scripts/*-smoke.mjs
